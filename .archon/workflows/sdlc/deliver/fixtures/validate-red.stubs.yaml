# Dry-run fixture: the validate-RED path — initial review is clean but the project
# gate fails, so the run fails at gate-validated (the new gate seen red).
# Run with --exec-code: gate-validated must EXECUTE to be seen red.
impl__record-start: '{"start":"recorded"}'
impl__implement:
  done: true
  green: true
  summary: "stub: implemented and green"
impl__record-green: '{"green":"recorded"}'
impl__assert-changed: "3 files changed"
gate-green: '{"gate":"green"}'
pr__pr: "https://github.com/example/repo/pull/2 (draft)"
review__scope: "target recorded; head sha stubbed"
review__choose:
  run_tests: "false"
  run_errors: "false"
  run_comments: "false"
  run_types: "false"
  run_docs: "false"
  run_simplify: "false"
  reasoning: "stub: tiny diff"
review__code: "0 findings"
review__seams: "0 findings"
review__tests: "skipped by chooser"
review__errors: "skipped by chooser"
review__comments: "skipped by chooser"
review__types: "skipped by chooser"
review__docs: "skipped by chooser"
review__simplify: "skipped by chooser"
review__synthesize:
  ready: true
  findings_summary: "stub: clean on initial review"
initial-ready: '{"ready":"true"}'
gate-ready: '{"ready":"true"}'
validate__validate:
  green: false
  summary: "stub: tests failed — 3 red"
await-checks: "no checks configured on this repository — nothing to await"
flip-ready: "https://github.com/example/repo/pull/2"
