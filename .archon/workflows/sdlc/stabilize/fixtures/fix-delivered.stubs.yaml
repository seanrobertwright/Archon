# Sparse dry-run fixture: the assessment proves a real flake with a prescribed
# decoupling, so the full delivery tail runs and the outcome node verifies the
# result. Run with --default-stubs.
assess:
  action: fix
  summary: "stub: bimodal profile at the 5000 ms bound — an undeclared real database open stalls the mocked test; fix order stubs the module and removes the bound collision"
assert-intact: '{"intact":"true"}'
gate-fix: '{"gate":"fix"}'
deliver__impl__implement:
  done: true
  green: true
  summary: "stub: coupling removed, 20 consecutive runs green under the trigger condition, test still fails on its planted fault"
deliver__review__synthesize:
  ready: true
  action: none
  findings_summary: "stub: no open findings"
deliver__review-action: '{"ready":true,"action":"none"}'
deliver__validate__validate:
  green: true
  summary: "stub: project gate green"
outcome: "https://github.com/example/repo/pull/9"
ci-probe: '{"state":"concluded","detail":"all checks green"}'
deliver__classify:
  errors: false
  docs: false
  reasons:
    errors: "stub: no failure paths"
    docs: "stub: no doc-facing changes"
deliver__resolve-scope: '{"errors":"false"}'
fixture:
  expect: completed
deliver__gate-ready: '{"gate":"ready"}'
deliver__impl__record-start: '{"start":"recorded"}'
deliver__gate-validated: '{"gate":"green"}'
deliver__impl__assert-changed: "2 files changed"
deliver__gate-green: '{"gate":"green"}'
deliver__flip-ready: "https://github.com/example/repo/pull/1"
deliver__outcome: "https://github.com/example/repo/pull/1"
deliver__pr__pr: { number: 1, url: "https://github.com/example/repo/pull/1", head: "fixture-branch", base: "dev", is_draft: true }
deliver__review__target: "PR #1 on fixture-branch"
deliver__review__mode: '{"continuation":false}'
deliver__review__scope: "target recorded; head sha stubbed"
deliver__review__code: "0 findings"
deliver__review__seams: "0 findings"
deliver__review__simplify: "0 findings"
deliver__review__tests: "0 findings"
deliver__review__review-complete: "settled"
