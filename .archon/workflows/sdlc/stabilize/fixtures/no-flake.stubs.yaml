# Sparse guard fixture: run with --default-stubs --exec-code. The assessment is
# explicit, every delivery node stays skipped, and the real outcome node returns
# the no_action reason with the report path.
assess:
  action: no_action
  summary: "stub: the named test was already stabilized on the current head — the sleep-based wait it flaked on no longer exists"
assert-intact: '{"intact":"true"}'
fixture:
  expect: completed
exec-code: true
