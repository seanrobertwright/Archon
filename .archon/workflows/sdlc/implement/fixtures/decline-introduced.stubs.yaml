# Dry-run fixture: the guard seen RED on introduced red (run with --exec-code).
# Nothing to show is tolerated only when the remaining red is not the change's own
# doing. Here it is — the branch broke the check and this round could not fix it — so
# the evidence behind the claim buys nothing: record-start and assert-changed execute
# for real and the run fails, exactly as it did before the non-introduced tolerance
# existed.
implement:
  done: true
  green: false
  red_cause: introduced
  summary: "stub: the failing suite is red on the branch's own regression from an earlier round, and this round could not fix it"
fixture:
  expect: failed
  fail-node: assert-changed
exec-code: true
