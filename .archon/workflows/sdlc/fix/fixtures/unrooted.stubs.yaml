# Dry-run fixture: the spend gate seen RED (run with --exec-code). The
# investigation is stubbed as unrooted; gate-rooted executes for real and must
# fail the run — an unrooted cause never reaches the delivery tail's spend.
# The composed delivery block stays skipped; the proof this fixture owns is
# gate-rooted's red.
inv__record-state: '{"state":"recorded"}'
inv__investigate:
  rooted: false
  summary: "stub: two hypotheses survive — not proven"
inv__assert-intact: '{"intact":"true"}'
